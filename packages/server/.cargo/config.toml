[build]
# Keep the workspace's target dir out of the git tree's hot paths.
[target.x86_64-unknown-linux-musl]
# Production target (Hetzner CX23). Build with: cargo zigbuild --release --target x86_64-unknown-linux-musl
